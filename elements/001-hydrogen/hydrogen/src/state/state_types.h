/*
 * Shared State Types
 *
 * This module defines common types used across the state management system.
 * These types are shared between the launch system and subsystem registry
 * to avoid circular dependencies.
 */

#ifndef STATE_TYPES_H
#define STATE_TYPES_H

// Include registry types
#include "../registry/registry.h"

#endif /* STATE_TYPES_H */