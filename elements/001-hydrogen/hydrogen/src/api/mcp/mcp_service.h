/*
 * MCP Service API header for the Hydrogen Project.
 *
 * Public OpenAPI tag for MCP REST endpoints. The Streamable HTTP daemon
 * remains on MCP.Port; this surface is JWT status on the WebServer API.
 */

#ifndef HYDROGEN_MCP_SERVICE_H
#define HYDROGEN_MCP_SERVICE_H

//@ swagger:service MCP Service
//@ swagger:description Authenticated MCP subsystem status for operators. The MCP JSON-RPC listen is a dedicated daemon, not these REST paths.
//@ swagger:version 1.0.0
//@ swagger:tag "MCP Service" Authenticated MCP health and counters on the WebServer API.

#endif /* HYDROGEN_MCP_SERVICE_H */
