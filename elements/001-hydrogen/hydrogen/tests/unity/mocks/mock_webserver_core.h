/*
 * Mock webserver core functions for unit testing
 *
 * Retained for CMake Unity mock linkage; former path helpers were removed
 * from production (resolve_webroot_path / get_payload_subdirectory_path /
 * resolve_filesystem_path). Macros stay no-ops unless revived.
 */

#ifndef MOCK_WEBSERVER_CORE_H
#define MOCK_WEBSERVER_CORE_H

#include <src/payload/payload.h>
#include <src/config/config.h>

#endif /* MOCK_WEBSERVER_CORE_H */
