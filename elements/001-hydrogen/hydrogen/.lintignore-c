# Cppcheck Configuration File (.lintignore-c)
# Modified to help identify unused .c files in a large project.
# Runs with broader checks and logs all processed files for later comparison with build output.
#
# SUMMARY: This config enables all checks, logs every file processed, and keeps key suppressions.
# Use with a script to compare against Makefile-compiled files.

# Enable all checks for maximum visibility
enable=all

# Include paths (adjust these to your system)
include=/usr/include
include=/usr/local/include

# Check level
check-level=exhaustive

# Custom template to log every file processed (even if no issues)
template={file}:{line}:{column}: {severity}: {message} ({id})
# Add a file-processed marker for scripting
template-file-processed={file}: PROCESSED

# Suppress specific warnings (kept most of yours, loosened usage-related ones)
suppress=missingIncludeSystem
suppress=constParameterCallback
suppress=constVariablePointer
suppress=normalCheckLevelMaxBranches
suppress=shadowVariable
suppress=knownConditionTrueFalse
suppress=redundantCondition
suppress=varFuncNullUB
suppress=redundantInitialization
suppress=unsignedLessThanZero
suppress=nullPointerRedundantCheck
suppress=nullPointerArithmeticRedundantCheck
suppress=constParameterPointer
suppress=redundantAssignment

# Additional options
option=--inline-suppr
option=--quiet
option=--file-list=src_files.txt  # Optional: feed it a list of .c files
option=--output-file=cppcheck_log.txt  # Save output for analysis